% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manuscript_standalone.R
\name{generate_manuscript_standalone}
\alias{generate_manuscript_standalone}
\title{Generate manuscript draft from RNAseqEr analysis results}
\usage{
generate_manuscript_standalone(
  save_dir,
  dir_lab = "all_celltypes",
  conditions_of_interest = NULL,
  tissue_type = "Brain",
  species = "Human",
  project_title = "Single-cell RNA-seq Analysis",
  researcher_name = "Researcher",
  journal_target = "bioRxiv",
  llm_provider = "openai",
  api_key = NULL,
  model_name = "gpt-4",
  max_tokens = 4000,
  temperature = 0.7,
  include_figures = TRUE,
  focus_on_genes = "condition",
  min_logfc = 0.5,
  max_pvalue = 0.05,
  auto_load_env = TRUE
)
}
\arguments{
\item{save_dir}{Directory where RNAseqEr analysis results are saved}

\item{dir_lab}{Label used for the analysis (default: "all_celltypes")}

\item{conditions_of_interest}{Vector of condition variables to focus on (e.g., c("AgeGroup", "Tissue"))}

\item{tissue_type}{Type of tissue analyzed (default: "Brain")}

\item{species}{Species analyzed (default: "Human")}

\item{project_title}{Title for the manuscript (default: "Single-cell RNA-seq Analysis")}

\item{researcher_name}{Name of the researcher (default: "Researcher")}

\item{journal_target}{Target journal for manuscript style (default: "bioRxiv")}

\item{llm_provider}{LLM provider to use ("openai", "anthropic", or "local")}

\item{api_key}{API key for LLM provider (if NULL, will try to load from environment)}

\item{model_name}{Model name to use (default: "gpt-4")}

\item{max_tokens}{Maximum tokens for LLM response (default: 4000)}

\item{temperature}{Temperature for LLM generation (default: 0.7)}

\item{include_figures}{Whether to include figure selection (default: TRUE)}

\item{focus_on_genes}{Focus on specific gene categories ("condition", "cluster", or "all")}

\item{min_logfc}{Minimum log fold change for gene selection (default: 0.5)}

\item{max_pvalue}{Maximum p-value for gene selection (default: 0.05)}

\item{auto_load_env}{Whether to automatically load environment variables (default: TRUE)}
}
\value{
Path to the manuscript directory
}
\description{
Standalone function to generate manuscript drafts from existing RNAseqEr analysis results.
This function can be used independently of the full quick_RNAseqEr workflow.
}
\examples{
\dontrun{
# Generate manuscript from existing analysis
manuscript_dir <- generate_manuscript_standalone(
  save_dir = "analysis_results",
  dir_lab = "all_celltypes",
  conditions_of_interest = c("AgeGroup", "Tissue"),
  project_title = "Age-related changes in brain tissue",
  researcher_name = "Dr. Smith",
  journal_target = "Nature Communications",
  llm_provider = "openai",
  api_key = "your-api-key-here"
)

# Use with environment variables
manuscript_dir <- generate_manuscript_standalone(
  save_dir = "analysis_results",
  conditions_of_interest = c("AgeGroup"),
  auto_load_env = TRUE  # Will load from .env file
)
}

}
